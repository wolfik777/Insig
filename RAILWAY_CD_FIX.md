# 🔧 Исправление: "The executable `cd` could not be found"

## ❌ Проблема

Railway не может выполнить команду `cd backend && python app.py` потому что `cd` не является исполняемым файлом в контексте Railway.

## ✅ Решение

### Вариант 1: Изменить Start Command в Railway (Рекомендуется)

1. Откройте Railway → ваш проект
2. **Settings** → **Service**
3. Измените **Start Command** на:
   ```
   python backend/app.py
   ```
   (без `cd backend &&`)
4. Нажмите **"Save"**
5. Нажмите **"Redeploy"**

### Вариант 2: Установить Root Directory

1. В Railway Settings → Service
2. **Root Directory:** установите `backend/`
3. **Start Command:** `python app.py`
4. **Save** и **Redeploy**

⚠️ **Но тогда нужно изменить пути к файлам!**

### Вариант 3: Использовать Dockerfile

1. В Railway Settings → Service
2. **Builder:** выберите **"Dockerfile"**
3. Railway будет использовать `Dockerfile` (уже настроен)
4. **Redeploy**

---

## ✅ Что исправлено в коде:

- ✅ `nixpacks.toml` - команда изменена на `python backend/app.py`
- ✅ `railway.json` - команда изменена
- ✅ `Procfile` - команда изменена
- ✅ `backend/app.py` - добавлена поддержка запуска из корня проекта

---

## 🚀 После исправления:

1. Обновите код на GitHub:
```bash
git add .
git commit -m "Fix Railway start command"
git push
```

2. В Railway нажмите **"Redeploy"**

3. Проверьте логи - должно работать!

---

## ✅ Проверка:

После успешного деплоя откройте:
`https://your-app.railway.app/api/health`

Должен вернуться: `{"status": "ok"}`

