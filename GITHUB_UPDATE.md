# 🔄 Обновление проекта на GitHub

## Если вы уже загрузили проект на GitHub

### Обновить проект (добавить новые изменения):

```bash
git add .
git commit -m "Update: исправления и улучшения"
git push
```

### Запустить локально:

```bash
# 1. Запустите Backend (в одном терминале)
cd backend
python app.py

# 2. Запустите Frontend (в другом терминале)
npm run dev
```

### Или используйте скрипты запуска:

**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

---

## Если GitHub просит что-то запустить

### Вариант 1: GitHub Actions (автоматическая проверка)

GitHub может предложить настроить GitHub Actions. Это необязательно, но полезно для автоматической проверки кода.

**Если хотите включить:**
- Файл `.github/workflows/deploy.yml` уже создан
- GitHub автоматически запустит проверки при каждом push

### Вариант 2: GitHub Pages (для статики)

Если хотите опубликовать Frontend на GitHub Pages:

1. Перейдите в Settings → Pages
2. Source: Deploy from a branch
3. Branch: `main` / `root`
4. Нажмите Save

**Но лучше использовать Vercel** (см. START_HERE.md)

---

## Быстрая команда для обновления:

```bash
# Добавить все изменения
git add .

# Закоммитить
git commit -m "Update project"

# Отправить на GitHub
git push
```

---

## Проверка статуса:

```bash
# Посмотреть что изменилось
git status

# Посмотреть изменения
git diff
```

---

## Если что-то пошло не так:

```bash
# Отменить последний commit (но оставить изменения)
git reset --soft HEAD~1

# Или отменить все изменения
git reset --hard HEAD
```

