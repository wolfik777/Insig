# 🔧 Исправление проблемы подключения Vercel к Railway

## ❌ Проблема
Frontend на Vercel не подключается к Backend на Railway, хотя при прямом переходе на API все работает.

## ✅ Решение

### 1. Проверьте переменную окружения в Vercel

1. Откройте проект в Vercel
2. Перейдите в **Settings** → **Environment Variables**
3. Убедитесь что есть переменная:
   - **Name:** `VITE_API_URL`
   - **Value:** `https://web-production-3f480.up.railway.app/api`
   - ⚠️ **Важно:** 
     - Должен быть `https://` в начале!
     - Должен быть `/api` в конце!
   - ✅ **Правильно:** `https://web-production-3f480.up.railway.app/api`
   - ❌ **Неправильно:** `web-production-3f480.up.railway.app/api` (без https://)
   - ❌ **Неправильно:** `https://web-production-3f480.up.railway.app` (без /api)

### 2. Передеплойте проект

**Обязательно передеплойте после изменения переменных окружения!**

1. Перейдите в **Deployments**
2. Нажмите **"..."** на последнем деплое
3. Выберите **"Redeploy"**
4. Дождитесь завершения деплоя

### 3. Проверьте что Backend работает

Откройте в браузере: `https://web-production-3f480.up.railway.app/api/health`

Должен вернуться:
```json
{"status": "ok", "message": "Insight API is running"}
```

### 4. Проверьте консоль браузера

1. Откройте ваш сайт на Vercel
2. Нажмите F12 (открыть DevTools)
3. Перейдите в вкладку **Console**
4. Посмотрите на ошибки (если есть)

### 5. Проверьте Network вкладку

1. В DevTools перейдите в **Network**
2. Попробуйте загрузить пример транзакции
3. Посмотрите на запросы к API
4. Проверьте:
   - URL запроса (должен быть `https://web-production-3f480.up.railway.app/api/...`)
   - Статус ответа (должен быть 200)
   - CORS заголовки (если есть ошибка CORS)

---

## 🔍 Что я исправил в коде

1. **CORS** - теперь разрешены все домены (`origins: "*"`)
2. **Отладка** - добавлено логирование API URL в консоль
3. **Сообщения об ошибках** - улучшены для Vercel

---

## 🐛 Если все еще не работает

### Проблема: Переменная окружения не применяется

1. Убедитесь что переменная добавлена для всех окружений: **Production**, **Preview**, **Development**
2. Передеплойте проект после добавления переменной
3. Проверьте что переменная видна в сборке (в Vercel → Settings → Environment Variables)

### Проблема: CORS ошибки

CORS уже настроен на разрешение всех доменов. Если все равно есть ошибки:
1. Проверьте что Backend действительно запущен на Railway
2. Проверьте логи Railway на наличие ошибок

### Проблема: 404 ошибки

1. Убедитесь что в переменной `VITE_API_URL` есть `/api` в конце
2. Проверьте что Backend доступен по адресу `https://web-production-3f480.up.railway.app/api/health`

---

## 📝 Быстрая проверка

1. ✅ Backend работает: `https://web-production-3f480.up.railway.app/api/health`
2. ✅ Переменная `VITE_API_URL` установлена в Vercel
3. ✅ Проект передеплоен после изменения переменной
4. ✅ Проверьте консоль браузера на ошибки

Готово! 🎉

