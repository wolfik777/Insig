# 📋 Пошаговая инструкция: Загрузка и запуск сервера

## 🎯 План действий

1. **Frontend** → Vercel (уже настроено)
2. **Backend** → Railway (автоматический запуск)
3. **Связать** их вместе

---

## Шаг 1: Загрузите код на GitHub (если еще не загрузили)

```bash
git add .
git commit -m "Ready for deployment"
git push
```

---

## Шаг 2: Деплой Backend на Railway (5 минут)

### 2.1. Создайте аккаунт на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Войдите через **GitHub**
4. Разрешите доступ к репозиториям

### 2.2. Деплой проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите репозиторий **`insight`**
4. Railway автоматически:
   - Определит Python
   - Установит зависимости из `backend/requirements.txt`
   - Запустит `python backend/app.py`
   - Сервер будет работать 24/7

### 2.3. Получите URL Backend

1. После деплоя Railway покажет URL
2. Пример: `https://insight-production.up.railway.app`
3. **Скопируйте этот URL!** Он понадобится для Frontend

### 2.4. Проверьте что Backend работает

Откройте в браузере: `https://your-backend-url.railway.app/api/health`

Должен вернуться:
```json
{"status": "ok", "message": "Insight API is running"}
```

✅ **Backend запущен и работает!**

---

## Шаг 3: Настройте Frontend на Vercel

### 3.1. Подключите репозиторий к Vercel

1. Откройте [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите репозиторий **`insight`**
5. Настройки (обычно определяются автоматически):
   - **Framework Preset:** Vite
   - **Root Directory:** `./`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
6. Нажмите **"Deploy"**

✅ **Frontend задеплоен!** URL: `https://insight.vercel.app`

### 3.2. Добавьте переменную окружения

1. В проекте Vercel перейдите в **Settings** → **Environment Variables**
2. Нажмите **"Add New"**
3. Заполните:
   - **Name:** `VITE_API_URL`
   - **Value:** `https://your-backend-url.railway.app/api`
   - (Замените на ваш URL из Railway)
4. Выберите **Production**, **Preview**, **Development**
5. Нажмите **"Save"**

### 3.3. Передеплойте Frontend

1. Перейдите в **Deployments**
2. Нажмите **"..."** на последнем деплое
3. Выберите **"Redeploy"**
4. Или сделайте новый commit и push (автоматический редеплой)

---

## Шаг 4: Проверка

### Проверьте Frontend:
- Откройте: `https://insight.vercel.app`
- Должен быть зеленый индикатор "Подключено к Backend"

### Проверьте Backend:
- Откройте: `https://your-backend-url.railway.app/api/health`
- Должен вернуться JSON с `"status": "ok"`

### Проверьте связь:
- На Frontend нажмите "Загрузить пример банковской выписки"
- Должны загрузиться примеры из Backend

---

## ✅ Готово!

Теперь у вас:
- ✅ Frontend работает на Vercel
- ✅ Backend работает на Railway
- ✅ Сервер запускается автоматически
- ✅ Все связано и работает

---

## 🔄 Обновление проекта

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

**Автоматически:**
- Railway передеплоит Backend
- Vercel передеплоит Frontend

---

## 🐛 Если что-то не работает

### Backend не запускается на Railway:

1. Проверьте логи в Railway (вкладка "Deployments" → "View Logs")
2. Убедитесь, что файл `backend/app.py` существует
3. Проверьте, что `backend/requirements.txt` содержит все зависимости

### Frontend не подключается к Backend:

1. Проверьте переменную окружения `VITE_API_URL` в Vercel
2. Убедитесь, что URL правильный (должен заканчиваться на `/api`)
3. Проверьте CORS настройки в Backend (уже настроено)

### CORS ошибки:

В `backend/app.py` уже настроен CORS для Vercel. Если проблемы:
- Убедитесь, что в CORS добавлен ваш Vercel домен
- Проверьте, что Backend доступен по HTTPS

---

## 📝 Итоговая структура

```
GitHub Repository
    ↓
    ├── Frontend → Vercel → https://insight.vercel.app
    │                    ↓
    │              API запросы
    │                    ↓
    └── Backend → Railway → https://your-backend.railway.app/api
```

**Оба сервера запускаются автоматически!** 🚀

