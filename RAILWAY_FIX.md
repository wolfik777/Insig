# 🔧 Исправление ошибки Railway "Failed to build an image"

## Проблема

Railway не может собрать образ из-за неправильной конфигурации.

## ✅ Решение

### Вариант 1: Использовать nixpacks.toml (Рекомендуется)

Файл `nixpacks.toml` уже создан. Railway автоматически его использует.

**Если все еще не работает:**

1. В Railway откройте проект
2. Перейдите в **Settings** → **Service**
3. Убедитесь, что:
   - **Root Directory:** `./` (корень проекта)
   - **Start Command:** `cd backend && python app.py`

### Вариант 2: Создать Procfile в корне

Создайте файл `Procfile` в корне проекта (не в backend):

```
web: cd backend && python app.py
```

### Вариант 3: Использовать Dockerfile

Создайте `Dockerfile` в корне проекта:

```dockerfile
FROM python:3.9-slim

WORKDIR /app

# Копируем requirements
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копируем весь backend
COPY backend/ ./backend/

# Устанавливаем рабочую директорию
WORKDIR /app/backend

# Открываем порт
EXPOSE 5000

# Запускаем приложение
CMD ["python", "app.py"]
```

Затем в Railway:
- **Settings** → **Service**
- **Build Command:** оставьте пустым (Dockerfile автоматически)
- **Start Command:** оставьте пустым

---

## 🔍 Проверка логов

1. В Railway откройте **Deployments**
2. Нажмите на последний деплой
3. Посмотрите **Build Logs** - там будет точная ошибка

---

## 🎯 Быстрое решение

### Шаг 1: Убедитесь что файлы на месте

```bash
# Проверьте что есть:
- backend/app.py
- backend/requirements.txt
- nixpacks.toml (в корне)
```

### Шаг 2: В Railway Settings

1. **Root Directory:** `./`
2. **Start Command:** `cd backend && python app.py`
3. **Build Command:** оставьте пустым (Railway определит автоматически)

### Шаг 3: Передеплой

1. В Railway нажмите **"Redeploy"**
2. Или сделайте новый commit и push

---

## 📝 Альтернатива: Использовать Heroku

Если Railway не работает, используйте Heroku:

1. Установите Heroku CLI
2. `heroku login`
3. `heroku create insight-backend`
4. `git push heroku main`

Heroku использует `Procfile` который уже настроен.

---

## ✅ Проверка

После успешного деплоя:
- Откройте URL Railway
- Добавьте `/api/health`
- Должен вернуться: `{"status": "ok"}`

