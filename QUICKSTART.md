# 🚀 Быстрый старт

## За 5 минут до первого запуска

### 1. Установите зависимости Backend

```bash
cd backend
pip install -r requirements.txt
```

### 2. Запустите Backend

```bash
python app.py
```

Должно появиться: `🚀 Insight Backend API запущен на http://localhost:5000`

### 3. В новом терминале - установите зависимости Frontend

```bash
# Вернитесь в корневую папку
cd ..
npm install
```

### 4. Запустите Frontend

```bash
npm run dev
```

Откройте браузер: `http://localhost:3000`

### 5. Протестируйте приложение

1. Загрузите тестовый файл: `backend/example_transactions.csv`
2. Посмотрите на виджет светофора
3. Изучите графики и статистику

## ✅ Готово!

Теперь вы можете загружать свои файлы с транзакциями.

## 🐛 Проблемы?

### Network Error / Не подключается к Backend

**Симптомы:** Красный индикатор "Не удалось подключиться к Backend"

**Решение:**
1. Убедитесь, что Backend запущен: `cd backend && python app.py`
2. Должно быть сообщение: `🚀 Insight Backend API запущен на http://localhost:5000`
3. Если порт другой (например, 5001), обновите `vite.config.js`:
   ```javascript
   target: 'http://localhost:5001'  // Измените на нужный порт
   ```
4. Перезапустите Frontend

### Backend не запускается
- Проверьте, что Python 3.9+ установлен: `python --version`
- Убедитесь, что все зависимости установлены: `pip list`
- Если порт 5000 занят, закройте процесс или используйте другой порт

### Frontend не запускается
- Проверьте Node.js: `node --version` (должно быть 16+)
- Удалите `node_modules` и установите заново: `rm -rf node_modules && npm install`

### Файл не загружается
- Проверьте формат файла (CSV, Excel, JSON, TXT)
- Убедитесь, что Backend запущен (зеленый индикатор в интерфейсе)
- Проверьте консоль браузера на ошибки (F12)

## 📞 Нужна помощь?

Откройте Issue в репозитории или свяжитесь с автором.

