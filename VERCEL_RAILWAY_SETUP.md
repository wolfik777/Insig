# 🚀 Быстрая настройка Vercel + Railway

## ✅ Backend уже работает на Railway!

Ваш Backend доступен по адресу: **`https://web-production-3f480.up.railway.app`**

---

## 📋 Шаг 1: Настройка Vercel для Frontend

### 1.1. Подключите репозиторий к Vercel

1. Откройте [vercel.com](https://vercel.com)
2. Войдите через **GitHub**
3. Нажмите **"Add New Project"**
4. Выберите репозиторий **`insight`**
5. Настройки (автоматически определяются):
   - **Framework Preset:** Vite
   - **Root Directory:** `./`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
6. Нажмите **"Deploy"**

✅ Frontend будет доступен по адресу: `https://your-project.vercel.app`

---

## 🔗 Шаг 2: Свяжите Frontend с Backend

### 2.1. Добавьте переменную окружения в Vercel

1. В проекте Vercel перейдите в **Settings** → **Environment Variables**
2. Нажмите **"Add New"**
3. Заполните:
   - **Name:** `VITE_API_URL`
   - **Value:** `https://web-production-3f480.up.railway.app/api`
   - ⚠️ **Важно:** Добавьте `/api` в конце!
4. Выберите все окружения: **Production**, **Preview**, **Development**
5. Нажмите **"Save"**

### 2.2. Передеплойте Frontend

1. Перейдите в **Deployments**
2. Нажмите **"..."** на последнем деплое
3. Выберите **"Redeploy"**
4. Или сделайте новый commit и push (автоматический редеплой)

---

## ✅ Шаг 3: Проверка

### Проверьте Backend:
Откройте в браузере: `https://web-production-3f480.up.railway.app/api/health`

Должен вернуться:
```json
{"status": "ok", "message": "Insight API is running"}
```

### Проверьте Frontend:
1. Откройте ваш Vercel URL: `https://your-project.vercel.app`
2. Проверьте статус Backend (должен быть зеленый)
3. Попробуйте загрузить пример транзакции

---

## 🎯 Готово!

Теперь:
- **Frontend:** `https://your-project.vercel.app` (Vercel)
- **Backend:** `https://web-production-3f480.up.railway.app` (Railway)

Оба работают вместе! 🎉

---

## 🐛 Если не работает

### Проблема: CORS ошибки

CORS уже настроен в `backend/app.py` для всех доменов Vercel (`https://*.vercel.app`).

Если все равно есть ошибки:
1. Проверьте что переменная `VITE_API_URL` правильно установлена в Vercel
2. Убедитесь что в значении есть `/api` в конце
3. Передеплойте Frontend после изменения переменных

### Проблема: Frontend не подключается к Backend

1. Проверьте что Backend работает: `https://web-production-3f480.up.railway.app/api/health`
2. Проверьте переменную окружения `VITE_API_URL` в Vercel
3. Убедитесь что передеплоили Frontend после добавления переменной

---

## 📝 Структура

```
Frontend (Vercel)
    ↓ API запросы
Backend (Railway) → https://web-production-3f480.up.railway.app/api
```

