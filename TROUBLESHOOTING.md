# 🔧 Решение проблем

## Проблема: Ошибка доступа к сокету / Порт занят

### Симптомы:
```
OSError: [WinError 10048] Обычно разрешается только одно использование адреса сокета
```

### Решения:

#### 1. Автоматический поиск свободного порта
Приложение автоматически найдет свободный порт, если 5000 занят.

#### 2. Закрыть процесс, использующий порт

**Windows:**
```bash
# Найти процесс на порту 5000
netstat -ano | findstr :5000

# Убить процесс (замените PID на номер из предыдущей команды)
taskkill /PID <PID> /F
```

**Linux/Mac:**
```bash
# Найти процесс
lsof -i :5000

# Убить процесс
kill -9 <PID>
```

#### 3. Изменить порт вручную

**Вариант A: Через переменную окружения**

Windows:
```bash
set FLASK_PORT=8080
python backend/app.py
```

Linux/Mac:
```bash
export FLASK_PORT=8080
python backend/app.py
```

**Вариант B: Изменить в коде**

Отредактируйте `backend/app.py`:
```python
default_port = int(os.getenv('FLASK_PORT', 8080))  # Измените 5000 на 8080
```

И обновите `vite.config.js`:
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8080',  // Измените порт
    changeOrigin: true
  }
}
```

#### 4. Запуск от имени администратора

На Windows: Правый клик на PowerShell/CMD → "Запуск от имени администратора"

## Проблема: Network Error / Frontend не подключается к Backend

### Симптомы:
- Ошибка "Network Error" в консоли браузера
- Красный индикатор "Не удалось подключиться к Backend" в интерфейсе

### Решения:

1. **Проверьте, что Backend запущен**
   - Должно быть сообщение: `🚀 Insight Backend API запущен на http://localhost:5000`
   - Если порт другой, смотрите решение ниже

2. **Проверьте порт в vite.config.js**
   - Должно быть: `target: 'http://localhost:5000'`
   - Если Backend запустился на другом порту (например, 5001), обновите:
   ```javascript
   proxy: {
     '/api': {
       target: 'http://localhost:5001',  // Измените на нужный порт
       changeOrigin: true
     }
   }
   ```

3. **Используйте относительные пути (уже исправлено)**
   - Все запросы теперь идут через `/api` (прокси Vite)
   - Не нужно указывать `http://localhost:5000` напрямую

4. **Перезапустите оба сервера**
   - Остановите Backend (Ctrl+C)
   - Остановите Frontend (Ctrl+C)
   - Запустите сначала Backend, потом Frontend

5. **Проверьте индикатор статуса**
   - В интерфейсе есть индикатор подключения к Backend
   - Зеленый = подключено, Красный = ошибка подключения

## Проблема: Файлы не загружаются

### Решения:

1. **Проверьте формат файла**
   - Поддерживаются: CSV, Excel (XLSX), JSON, TXT

2. **Проверьте структуру данных**
   - Должны быть колонки: Дата, Сумма, Категория, Описание
   - Названия могут быть на русском или английском

3. **Проверьте кодировку**
   - Используйте UTF-8

4. **Проверьте размер файла**
   - Максимум 16MB

## Проблема: Примеры не загружаются

1. Проверьте, что папка `backend/examples/` существует
2. Проверьте, что файлы примеров на месте
3. Проверьте консоль браузера на ошибки (F12)

## Другие проблемы

### Python зависимости не установлены
```bash
cd backend
pip install -r requirements.txt
```

### Node модули не установлены
```bash
npm install
```

### Порт Frontend занят
Измените в `vite.config.js`:
```javascript
server: {
  port: 3001,  // Измените на другой порт
}
```

## Получение помощи

Если проблема не решена:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте логи Backend в терминале
3. Откройте Issue в репозитории с описанием проблемы

